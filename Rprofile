# change csv defaults that drive me nuts 
options(stringsAsFactors=F, row.names=F)
    
.First <- function(){
 cat("\nGettin my R on: ", date(), "\n") 
}

.Last <- function(){ 
 cat("\nGoodye R:  ", date(), "\n")
}
